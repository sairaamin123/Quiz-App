<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quiz_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quiz_App">
        <activity android:name=".Activities.QuestionActivity" />
        <activity android:name=".Activities.RateUsActivity" />
        <activity android:name=".Activities.FollowUsActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.LogninIntroActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.LogIn_Activity"></activity>
        <activity android:name=".Activities.MainActivity">

        </activity>
    </application>

</manifest>